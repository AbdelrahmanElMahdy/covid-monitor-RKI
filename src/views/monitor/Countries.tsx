import React, { ReactElement } from "react";
import Country from "../../components/monitor/Country";
const Countries: React.FC = (): ReactElement => {
  return (
    <Country/>
  );
};

export default Countries;
